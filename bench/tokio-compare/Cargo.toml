[package]
name = "tokio-compare"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time", "macros"] }

[profile.release]
opt-level = 3
codegen-units = 1
lto = "fat"
